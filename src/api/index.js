/**
 * Слой за достъп до данните.
 *
 * Страниците не викат `supabase` пряко. Всяка заявка живее тук, групирана по
 * тема. Ползата е конкретна:
 *   * ако утре се смени име на колона, се пипа един файл, не осем страници;
 *   * всяка функция хвърля грешка вместо да връща { data, error }, така че
 *     страниците не повтарят една и съща проверка след всяко извикване;
 *   * вижда се на едно място кое минава през таблица и кое през RPC функция.
 */
export * as classes from './classes.js';
export * as students from './students.js';
export * as quizzes from './quizzes.js';
export * as play from './play.js';
